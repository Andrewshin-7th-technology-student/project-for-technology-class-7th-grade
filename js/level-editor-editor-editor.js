// level-editor-editor-editor.js
// Level editor editor editing and saving

// Initialize level editor editor editor variables
let levelEditorEditorEditor;

// Initialize level editor editor editor
function initLevelEditorEditorEditor() {
  levelEditorEditorEditor = new LevelEditorEditorEditor();
}

// Edit level editor editor
function editLevelEditorEditor() {
  // Edit level editor editor
  levelEditorEditorEditor.edit();
}

// Save level editor editor
function saveLevelEditorEditor() {
  // Save level editor editor
  levelEditorEditorEditor.save();
}

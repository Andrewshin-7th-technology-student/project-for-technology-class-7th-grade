// level-selector-editor-editor.js
// Level selector editor editing and saving

// Initialize level selector editor editor variables
let levelSelectorEditorEditor;

// Initialize level selector editor editor
function initLevelSelectorEditorEditor() {
  levelSelectorEditorEditor = new LevelSelectorEditorEditor();
}

// Edit level selector editor
function editLevelSelectorEditor() {
  // Edit level selector editor
  levelSelectorEditorEditor.edit();
}

// Save level selector editor
function saveLevelSelectorEditor() {
  // Save level selector editor
  levelSelectorEditorEditor.save();
}

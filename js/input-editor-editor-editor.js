// input-editor-editor-editor.js
// Input editor editor editing and saving

// Initialize input editor editor editor variables
let inputEditorEditorEditor;

// Initialize input editor editor editor
function initInputEditorEditorEditor() {
  inputEditorEditorEditor = new InputEditorEditorEditor();
}

// Edit input editor editor
function editInputEditorEditor() {
  // Edit input editor editor
  inputEditorEditorEditor.edit();
}

// Save input editor editor
function saveInputEditorEditor() {
  // Save input editor editor
  inputEditorEditorEditor.save();
}

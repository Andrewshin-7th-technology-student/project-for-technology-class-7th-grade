// animation-manager-editor.js
// Animation manager editing and saving

// Initialize animation manager editor variables
let animationManagerEditor;

// Initialize animation manager editor
function initAnimationManagerEditor() {
  animationManagerEditor = new AnimationManagerEditor();
}

// Edit animation manager
function editAnimationManager() {
  // Edit animation manager
  animationManagerEditor.edit();
}

// Save animation manager
function saveAnimationManager() {
  // Save animation manager
  animationManagerEditor.save();
}

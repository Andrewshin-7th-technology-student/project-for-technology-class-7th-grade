// audio-editor-editor-editor.js
// Audio editor editor editing and saving

// Initialize audio editor editor editor variables
let audioEditorEditorEditor;

// Initialize audio editor editor editor
function initAudioEditorEditorEditor() {
  audioEditorEditorEditor = new AudioEditorEditorEditor();
}

// Edit audio editor editor
function editAudioEditorEditor() {
  // Edit audio editor editor
  audioEditorEditorEditor.edit();
}

// Save audio editor editor
function saveAudioEditorEditor() {
  // Save audio editor editor
  audioEditorEditorEditor.save();
}

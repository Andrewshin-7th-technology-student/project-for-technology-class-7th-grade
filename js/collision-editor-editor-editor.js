// collision-editor-editor-editor.js
// Collision editor editor editing and saving

// Initialize collision editor editor editor variables
let collisionEditorEditorEditor;

// Initialize collision editor editor editor
function initCollisionEditorEditorEditor() {
  collisionEditorEditorEditor = new CollisionEditorEditorEditor();
}

// Edit collision editor editor
function editCollisionEditorEditor() {
  // Edit collision editor editor
  collisionEditorEditorEditor.edit();
}

// Save collision editor editor
function saveCollisionEditorEditor() {
  // Save collision editor editor
  collisionEditorEditorEditor.save();
}

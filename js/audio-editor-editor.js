// audio-editor-editor.js
// Audio editor editing and saving

// Initialize audio editor editor variables
let audioEditorEditor;

// Initialize audio editor editor
function initAudioEditorEditor() {
  audioEditorEditor = new AudioEditorEditor();
}

// Edit audio editor
function editAudioEditor() {
  // Edit audio editor
  audioEditorEditor.edit();
}

// Save audio editor
function saveAudioEditor() {
  // Save audio editor
  audioEditorEditor.save();
}

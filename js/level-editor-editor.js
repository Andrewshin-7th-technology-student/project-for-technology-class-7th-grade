// level-editor-editor.js
// Level editor editing and saving

// Initialize level editor editor variables
let levelEditorEditor;

// Initialize level editor editor
function initLevelEditorEditor() {
  levelEditorEditor = new LevelEditorEditor();
}

// Edit level editor
function editLevelEditor() {
  // Edit level editor
  levelEditorEditor.edit();
}

// Save level editor
function saveLevelEditor() {
  // Save level editor
  levelEditorEditor.save();
}

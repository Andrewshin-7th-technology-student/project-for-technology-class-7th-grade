// image-editor-editor.js
// Image editor editing and saving

// Initialize image editor editor variables
let imageEditorEditor;

// Initialize image editor editor
function initImageEditorEditor() {
  imageEditorEditor = new ImageEditorEditor();
}

// Edit image editor
function editImageEditor() {
  // Edit image editor
  imageEditorEditor.edit();
}

// Save image editor
function saveImageEditor() {
  // Save image editor
  imageEditorEditor.save();
}

// level-selector-editor-editor-editor.js
// Level selector editor editor editing and saving

// Initialize level selector editor editor editor variables
let levelSelectorEditorEditorEditor;

// Initialize level selector editor editor editor
function initLevelSelectorEditorEditorEditor() {
  levelSelectorEditorEditorEditor = new LevelSelectorEditorEditorEditor();
}

// Edit level selector editor editor
function editLevelSelectorEditorEditor() {
  // Edit level selector editor editor
  levelSelectorEditorEditorEditor.edit();
}

// Save level selector editor editor
function saveLevelSelectorEditorEditor() {
  // Save level selector editor editor
  levelSelectorEditorEditorEditor.save();
}

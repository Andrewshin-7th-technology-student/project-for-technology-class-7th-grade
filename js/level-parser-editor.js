// level-parser-editor.js
// Level parser editing and saving

// Initialize level parser editor variables
let levelParserEditor;

// Initialize level parser editor
function initLevelParserEditor() {
  levelParserEditor = new LevelParserEditor();
}

// Edit level parser
function editLevelParser() {
  // Edit level parser
  levelParserEditor.edit();
}

// Save level parser
function saveLevelParser() {
  // Save level parser
  levelParserEditor.save();
}

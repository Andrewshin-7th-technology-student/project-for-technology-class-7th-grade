// input-editor-editor.js
// Input editor editing and saving

// Initialize input editor editor variables
let inputEditorEditor;

// Initialize input editor editor
function initInputEditorEditor() {
  inputEditorEditor = new InputEditorEditor();
}

// Edit input editor
function editInputEditor() {
  // Edit input editor
  inputEditorEditor.edit();
}

// Save input editor
function saveInputEditor() {
  // Save input editor
  inputEditorEditor.save();
}

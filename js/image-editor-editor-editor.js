// image-editor-editor-editor.js
// Image editor editor editing and saving

// Initialize image editor editor editor variables
let imageEditorEditorEditor;

// Initialize image editor editor editor
function initImageEditorEditorEditor() {
  imageEditorEditorEditor = new ImageEditorEditorEditor();
}

// Edit image editor editor
function editImageEditorEditor() {
  // Edit image editor editor
  imageEditorEditorEditor.edit();
}

// Save image editor editor
function saveImageEditorEditor() {
  // Save image editor editor
  imageEditorEditorEditor.save();
}

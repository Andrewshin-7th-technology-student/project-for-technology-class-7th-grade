// image-loader-editor.js
// Image loader editing and saving

// Initialize image loader editor variables
let imageLoaderEditor;

// Initialize image loader editor
function initImageLoaderEditor() {
  imageLoaderEditor = new ImageLoaderEditor();
}

// Edit image loader
function editImageLoader() {
  // Edit image loader
  imageLoaderEditor.edit();
}

// Save image loader
function saveImageLoader() {
  // Save image loader
  imageLoaderEditor.save();
}

// input-manager-editor.js
// Input manager editing and saving

// Initialize input manager editor variables
let inputManagerEditor;

// Initialize input manager editor
function initInputManagerEditor() {
  inputManagerEditor = new InputManagerEditor();
}

// Edit input manager
function editInputManager() {
  // Edit input manager
  inputManagerEditor.edit();
}

// Save input manager
function saveInputManager() {
  // Save input manager
  inputManagerEditor.save();
}

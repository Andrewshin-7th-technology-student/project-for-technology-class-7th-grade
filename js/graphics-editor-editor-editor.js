// graphics-editor-editor-editor.js
// Graphics editor editor editing and saving

// Initialize graphics editor editor editor variables
let graphicsEditorEditorEditor;

// Initialize graphics editor editor editor
function initGraphicsEditorEditorEditor() {
  graphicsEditorEditorEditor = new GraphicsEditorEditorEditor();
}

// Edit graphics editor editor
function editGraphicsEditorEditor() {
  // Edit graphics editor editor
  graphicsEditorEditorEditor.edit();
}

// Save graphics editor editor
function saveGraphicsEditorEditor() {
  // Save graphics editor editor
  graphicsEditorEditorEditor.save();
}

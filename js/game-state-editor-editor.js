// game-state-editor-editor.js
// Game state editor editing and saving

// Initialize game state editor editor variables
let gameStateEditorEditor;

// Initialize game state editor editor
function initGameStateEditorEditor() {
  gameStateEditorEditor = new GameStateEditorEditor();
}

// Edit game state editor
function editGameStateEditor() {
  // Edit game state editor
  gameStateEditorEditor.edit();
}

// Save game state editor
function saveGameStateEditor() {
  // Save game state editor
  gameStateEditorEditor.save();
}

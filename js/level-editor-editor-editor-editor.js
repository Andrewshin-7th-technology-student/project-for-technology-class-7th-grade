// level-editor-editor-editor-editor.js
// Level editor editor editor editing and saving

// Initialize level editor editor editor editor variables
let levelEditorEditorEditorEditor;

// Initialize level editor editor editor editor
function initLevelEditorEditorEditorEditor() {
  levelEditorEditorEditorEditor = new LevelEditorEditorEditorEditor();
}

// Edit level editor editor editor
function editLevelEditorEditorEditor() {
  // Edit level editor editor editor
  levelEditorEditorEditorEditor.edit();
}

// Save level editor editor editor
function saveLevelEditorEditorEditor() {
  // Save level editor editor editor
  levelEditorEditorEditorEditor.save();
}
